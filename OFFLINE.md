# Работа в оффлайн режиме

## Проверка оффлайн работы

Проект полностью работает без доступа к интернету после первоначальной сборки.

### Проверка зависимостей

```bash
# Проверка что все модули загружены
go mod verify

# Сборка без доступа к сети
GOPROXY=off go build

# Запуск в оффлайн режиме
./chess-ai
```

### Что работает оффлайн

✅ **Все режимы работы:**
- Веб-интерфейс (http://localhost:8080)
- Терминальный режим (`--terminal`)
- Режим самообучения (`--self-play`)

✅ **База данных:**
- SQLite файловая база (data/chess.db)
- Не требует внешних серверов
- Хранит все данные локально

✅ **Нейронная сеть:**
- Полностью реализована на Go
- Без внешних библиотек для ML
- Веса сохраняются локально (neural/weights.gob)

### Единственная зависимость

Проект использует только одну внешнюю библиотеку:
- `github.com/mattn/go-sqlite3` - SQLite драйвер для Go

Эта зависимость загружается при первом `go build` или `go get`, после чего хранится в локальном кеше Go модулей и доступна оффлайн.

### Тестирование оффлайн работы

```bash
# 1. Соберите проект с интернетом (один раз)
go build

# 2. Отключите интернет или установите GOPROXY=off

# 3. Пересоберите проект
GOPROXY=off go build

# 4. Запустите любой режим
./chess-ai --self-play --games 10
```

Все будет работать без проблем! 🎯
